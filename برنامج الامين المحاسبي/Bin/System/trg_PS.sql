Create TRIGGER trg_mnps_DeletePsChildren
						ON MNPS000
						FOR DELETE 
						NOT FOR REPLICATION
AS 
	IF @@ROWCOUNT = 0 RETURN
	SET NOCOUNT ON	

	DELETE PSI000
	FROM PSI000 P INNER JOIN DELETED D ON  P.PARENTGUID= D.GUID
